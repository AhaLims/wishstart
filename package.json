{
  "name": "wishstar",
  "version": "1.0.0",
  "description": "许愿星计划",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install --production",
    "migrate:export": "node scripts/export-redis.js"
  }
}
